{
  "name": "wedding-invitation",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "predev": "node scripts/generate-gallery-manifest.mjs",
    "dev": "vite",
    "prebuild": "node scripts/generate-gallery-manifest.mjs",
    "sync:gallery": "node scripts/generate-gallery-manifest.mjs",
    "build": "vite build",
    "preview": "vite preview"
  },
  "devDependencies": {
    "sharp": "^0.34.5",
    "vite": "^6.3.1"
  },
  "dependencies": {
    "canvas": "^3.2.3"
  }
}
